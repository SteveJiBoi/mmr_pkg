#!/usr/bin/env python3
"""Console entry point so `ros2 run mmr_pkg esp32_bridge` works.

Same shim pattern as scripts/kiwi_drive_node; see CMakeLists.txt for why an
ament_cmake package needs these by hand.
"""
from mmr_pkg.esp32_bridge import main

if __name__ == "__main__":
    main()
